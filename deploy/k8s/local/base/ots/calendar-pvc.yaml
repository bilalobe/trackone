apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ots-calendar-data
  namespace: trackone
  labels:
    app.kubernetes.io/name: ots-calendar
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
