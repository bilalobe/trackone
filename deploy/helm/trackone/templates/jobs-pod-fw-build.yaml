{{- if and .Values.jobs.enabled .Values.jobs.podFw.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: trackone-pod-fw-build
  namespace: {{ include "trackone.namespace" . }}
  labels:
    app.kubernetes.io/name: trackone-pod-fw-build
    {{ include "trackone.commonLabels" . | nindent 4 }}
spec:
  backoffLimit: {{ .Values.jobs.backoffLimit }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: trackone-pod-fw-build
        {{ include "trackone.commonLabels" . | nindent 8 }}
    spec:
      automountServiceAccountToken: false
      restartPolicy: Never
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: pod-fw-build
          image: {{ include "trackone.image" .Values.jobs.podFw.image }}
          imagePullPolicy: {{ .Values.jobs.podFw.image.pullPolicy }}
          env:
            - name: TRACKONE_POD_FW_FEATURES
              value: "production"
            - name: TRACKONE_POD_FW_NO_DEFAULT_FEATURES
              value: "1"
            - name: TRACKONE_POD_FW_RELEASE
              value: "1"
          resources:
            {{ toYaml .Values.jobs.podFw.resources | nindent 12 }}
{{- end }}
